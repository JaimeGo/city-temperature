let redisClient;_eb3‍.x([["getCoordinates",()=>getCoordinates],["registerApiError",()=>registerApiError]]);_eb3‍.w("./redisClient",[["default",["redisClient"],function(v){redisClient=v}]]);

       async function getCoordinates(cityName) {
	try {
		const coordinates = await redisClient.get(cityName);
		return JSON.parse(coordinates);
	} catch (error) {
		_eb3‍.g.console.error(error);
	}
}

       async function registerApiError(cityName) {
	try {
		return await redisClient.hset('api.errors', new Date(), cityName);
	} catch (error) {
		_eb3‍.g.console.error(error);
	}
}
